[tasks.install-dependencies]
script = [
    "cd packages/open-cleaner && yarn install"
]

[tasks.build-gui]
dependencies = ["install-dependencies"]
script = [
    "cd packages/open-cleaner && yarn build"
]

[tasks.build-all]
dependencies = ["build-gui"]
script = [
    "echo hello"
]